#include <stdio.h>
#define size 5
#include <stdlib.h>

int main()
{

    system("cls");
    struct linear_queue
    {
        int front;
        int rear;
        int item[size];
    };
    struct linear_queue qt;

    int rear, front;
    rear = -1;
    front = 0;

    return 0;
}